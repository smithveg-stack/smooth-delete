# Laravel example — planned (v0.2.0)

Not implemented yet. Contributions welcome — see [CONTRIBUTING.md](../../CONTRIBUTING.md).

Should include: a POST (or DELETE) route, a controller `destroy()` method,
form-request validation, CSRF (Laravel's default, don't disable it),
authorization via a policy/gate, a JSON response matching the contract in
the root README, a Blade view wiring up `assets/smooth-delete.js`, and
tests.
