# Python/Flask example — planned (v0.2.0)

Not implemented yet. Contributions welcome — see [CONTRIBUTING.md](../../CONTRIBUTING.md).

Should include: a Flask route, SQLite via parameterized queries, CSRF
protection, a Jinja2 template wiring up `assets/smooth-delete.js`, a
`requirements.txt`, and tests (pytest).
